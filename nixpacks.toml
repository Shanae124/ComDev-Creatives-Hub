[phases.setup]
nixPkgs = ["nodejs_20", "postgresql"]

[phases.install]
cmds = ["npm ci --include=dev || npm install"]

[phases.build]
env = { SKIP_ENV_VALIDATION = "true", NEXT_SKIP_TYPE_CHECK = "true" }
cmds = ["npm run build"]

[start]
cmd = "node start-production.js"
