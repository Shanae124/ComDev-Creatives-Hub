[phases.setup]
nixPkgs = ["nodejs_20", "postgresql"]

[phases.install]
cmds = ["npm ci --include=dev || npm install"]

[phases.build]
cmds = ["echo 'Skipping Next.js build - API only deployment'"]

[start]
cmd = "node server.js"
